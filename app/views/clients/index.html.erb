<div class="box bordered" style="position:relative;">
	<div class="box-header" >
		<i class="icon-th-list"></i><%= @page_title = "Clientes" %>
	</div>

	<table class="table table-striped data-table">
		<thead>
			<tr>
				<th>Ações</th>
			    <th>Nome</th>
			    <th>E-mail</th>
				<th>Telefone</th>
				<th>Tipo de cliente</th>
				<th>Instituição/Empresa</th>
			</tr>
		</thead>
		<tbody>
			<% @clients.each do |k, client| %>
			  <tr>
  			    <td class="td-actions">
  			        <%= link_to '', {:action => 'show', :id => client.id}, :class => "icon-eye-open", :title => "Visualizar" %>
  				</td>
			    <td><%= client.name %></td>
			    <td><%= client.email %></td>
				<td><%= client.phone %></td>
				<td><%= Client.client_types[client.clientType] %></td>
				<td><%= client.institution %></td>
			  </tr>
			<% end %>
		</tbody>
	</table>
</div>

<br />
