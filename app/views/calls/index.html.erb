<h1>Chamados</h1>

<table>
  <tr>
    <th>Motivo</th>
    <th>Comentários</th>
    <th>Status</th>
    <th>Data</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @calls.each do |call| %>
  <tr>
    <td><%= call.reason %></td>
    <td><%= call.comments %></td>
    <td><%= call.status %></td>
    <td><%= call.call_date %></td>
    <td><%= link_to 'Detalhes', call %></td>
    <td><%= link_to 'Editar', edit_call_path(call) %></td>
    <td><%= link_to 'Excluir', call, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Chamado', new_call_path %>
