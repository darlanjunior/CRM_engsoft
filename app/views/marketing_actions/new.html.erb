<h2><%= @page_title = "Nova Ação de Markting" %></h2><hr />

<%= form_for @marketing_action, :validate => true, :html => {:class => "form-horizontal"} do |f| %>
  
  <%= render(:partial => 'form', :locals => {:f => f}) %>    
  <div class="control-group">
    <div class="controls">
      <%= f.submit("Criar", :class => "btn btn-small dark-blue") %>
    </div>
  </div>
<% end %>

<%= link_to 'Ir para a lista de ações de marketing', marketing_actions_path, :class => "btn btn-small light-blue inverse" %>
