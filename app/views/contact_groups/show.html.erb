<h2><%= @page_title = "Visualizar Grupo de Contatos" %></h2><hr />

<% if notice %>
	<div class="alert green fade in">
		<button type="button" class="close" data-dismiss="alert">&times;</button>
		<%= notice %>
	</div>
<% end %>

<%= form_for @contact_group, :html => {:class => "form-horizontal"} do |f| -%>

  <%= render(:partial => 'form', :locals => {:f => f, :readonly => true}) %>    
  <div class="control-group">
    <div class="controls">
      <%= link_to 'Editar Grupo de Contatos', {:action => 'edit', :id => @contact_group.id}, :class => "btn btn-small dark-blue" %>
  </div>
<% end %>

<%= link_to 'Ir para a lista de grupos de contatos', contact_groups_path, :class => "btn btn-small light-blue inverse" %>
