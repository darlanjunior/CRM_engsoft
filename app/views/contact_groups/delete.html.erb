<h2><%= @page_title = "Deletar Grupo de Contatos" %></h2><hr />

<p class="red-text strong" style="font-size:16px">Você tem certeza que deseja deletar permanentemente esse grupo de contatos?</p>
<%= form_for @contact_group, :html => {:class => "form-horizontal"} do |f| -%>

  <%= render(:partial => 'form', :locals => {:f => f, :readonly => true}) %>    
  <div class="control-group">
	  <div class="controls">
		  <%= button_to "Deletar Grupo de Contatos", contact_group_path(@contact_group), :method => 'delete', :class => "btn btn-small dark-blue" %>
	  </div>
  </div>
<% end %>

<%= link_to 'Ir para a lista de grupos de contatos', contact_groups_path, :class => "btn btn-small light-blue inverse" %>
