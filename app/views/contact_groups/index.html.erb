<div class="box bordered" style="position:relative;">
	<div class="box-header" >
		<i class="icon-th-list"></i><%= @page_title = "Grupos de contatos" %>
	</div>

	<table class="table table-striped data-table">
		<thead>
		<tr>
			<th>Ações</th>
		  <th>Nome do Grupo</th>
		</tr>
		</thead>

		<tbody>
	<% @contact_groups.each do |contact_group| %>
		<tr>
			<td class="td-actions">
		  	<%= link_to '', {:action => 'show', :id => contact_group.id}, :class => "icon-eye-open", :title => "Visualizar" %>
		  	<%= link_to '', {:action => 'edit', :id => contact_group.id}, :class => "icon-edit", :title => "Editar" %>
		  	<%= link_to '', {:action => 'delete', :id => contact_group.id}, :class => "icon-remove", :title => "Deletar"  %>
		  </td>
		  <td><%= contact_group.name %></td>
		</tr>
	<% end %>
	</table>
</div>

<br />

<%= link_to 'Novo grupo de contatos', {:action => 'new'}, :class => "btn btn-small light-blue inverse"  %>
