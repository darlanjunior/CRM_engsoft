<%= form_for(@contact_group) do |f| %>
  <% if @contact_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_group.errors.count, "error") %> prohibited this contact_group from being saved:</h2>

      <ul>
      <% @contact_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

	<br/> 

	<h3>Contatos</h3>
  <div class="vpadded">
 <% @contacts.each do |contact| %>
		<%= check_box_tag("contacts[" + contact.id.to_s + "]", :class => "checkbox") %>
		
		<%= contact.name %>
		<br/>
<% end %>
	</div>
	
	<div class="actions">
    <%= f.submit 'Salvar', :class => 'btn btn-small dark-blue' %>
  </div>

</div>
<% end %>
