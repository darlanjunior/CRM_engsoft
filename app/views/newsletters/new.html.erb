<h2><%= @page_title = "Nova Newsletter" %></h2><hr />

<%= form_for @newsletter, :validate => true, :html => {:class => "form-horizontal"} do |f| %>
  
  <%= render(:partial => 'form', :locals => {:f => f}) %>    
  <div class="control-group">
    <div class="controls">
      <%= f.submit("Criar Newsletter", :class => "btn btn-small dark-blue") %>
    </div>
  </div>
<% end %>

<%= link_to '<< Voltar', newsletters_path, :class => "btn btn-small light-blue inverse" %>
