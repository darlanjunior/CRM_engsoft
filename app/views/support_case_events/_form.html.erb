<%= form_for(@support_case_event) do |f| %>
  <% if @support_case_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@support_case_event.errors.count, "error") %> prohibited this support_case_event from being saved:</h2>

      <ul>
      <% @support_case_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
