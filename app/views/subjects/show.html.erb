<h2><%= @page_title = "Visualizar Tópico" %></h2><hr />

<% if notice %>
	<div class="alert green fade in">
		<button type="button" class="close" data-dismiss="alert">&times;</button>
		<%= notice %>
	</div>
<% end %>

<%= form_for @subject, :html => {:class => "form-horizontal"} do |f| -%>

  <%= render(:partial => 'form', :locals => {:f => f, :readonly => true}) %> 
  <%= render(:partial => 'partials/timestamps', :locals => {:obj => @subject}) %>
  <div class="control-group">
    <div class="controls">
      <%= link_to 'Editar Tópico', {:action => 'edit', :id => @subject.id}, :class => "btn btn-small dark-blue" %>
  </div>
<% end %>

<br />
<%= link_to 'Ir para a lista de tópicos', subjects_path, :class => "btn btn-small light-blue inverse" %>&nbsp; &nbsp;&nbsp;
<%= link_to 'Novo tópico', new_subject_path, :class => "btn btn-small light-blue inverse" %>
